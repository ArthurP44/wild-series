<nav class="navbar navbar-expand-lg navbar-light bg-dark fixed-top">
    <a class="navbar-brand btn btn-primary text-white" href="{{ path('app_index') }}">Accueil</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link btn btn-outline-primary text-white mr-2" href="{{ path('program_index') }}">Séries</a>
            </li>
            <li class="nav-item">
                {{ render(controller('App\\Controller\\CategoryController::listCategories')) }}
            </li>
            <li class="nav-item">
                <a class="nav-link btn btn-outline-primary text-white mr-2" href="{{ path('season_index') }}">Saisons</a>
            </li>
            <li class="nav-item">
                <a class="nav-link btn btn-outline-primary text-white mr-2" href="{{ path('episode_index') }}">Épisodes</a>
            </li>
            <li class="nav-item">
                <a class="nav-link btn btn-outline-primary text-white mr-2" href="{{ path('actor') }}">Acteurs</a>
            </li>
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link btn btn-outline-warning text-white mr-2" href="{{ path('account') }}">Mon compte</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-outline-danger text-white mr-2" href="{{ path('app_logout') }}">{{ 'account.logout'|trans }}</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link btn btn-outline-success text-white mr-2" href="{{ path('app_login') }}">{{ 'account.login'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-outline-warning text-white mr-2" href="{{ path('app_register') }}">S'inscrire</a>
                </li>
            {% endif %}

            {% if app.user and is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link btn btn-outline-warning text-white mr-2" href="">Administration</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>.